id: lw:0004
kind: regex
target: file_php
pattern: 'preg_replace\s*\(\s*["''][^"'']*\/[a-zA-Z]*e[a-zA-Z]*["'']'
flags: ''
sql_like: null
tier: infected
category: backdoor
name: preg_replace() with the removed /e (eval) modifier
description: preg_replace() called with a pattern ending in the legacy /e modifier, which executed the replacement as PHP code (remote-code-execution primitive on PHP < 7).
common_strings: []
tests:
  match:
    - "preg_replace('/(.*)/e', '\\1', $input);"
    - 'preg_replace("/x/ie", $pat, $str);'
  no_match:
    - "preg_replace('/x/i', 'y', $str);"
    - "preg_replace('/^\\s+/', '', $line);"
